services:
  # service API
  api:
    build: ./budget-app-api
    ports:
      - "8000:80"
    environment:
      - APP_ENV=dev
      - APP_DEBUG=true
      - DB_HOST=db
    depends_on:
      - db
    volumes:
      - ./budget-app-api:/var/www # Bind Mount (hot reload) : for development
    networks:
      - budget-network

  # service frontend

  # service database
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-securepassword}
      - POSTGRES_DB=${POSTGRES_DB:-budget_db}
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - budget-network

  # service Mailhog
  mailhog:
    image: mailhog/mailhog:v1.0.1
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - budget-network

volumes:
  db_data:

networks:
  budget-network:
