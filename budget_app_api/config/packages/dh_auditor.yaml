dh_auditor:
    enabled: true
    timezone: 'UTC'

    providers:
        doctrine:
            # Activer le viewer d'audit
            viewer: true

            # Configuration des entités à auditer
            entities:
                App\Entity\User: ~
